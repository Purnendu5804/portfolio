export const certificates: {
  file: string;
  title: string;
  issuer: string;
  date: string;
}[] = [
  // TODO: Add real certificates once provided.
  // Leave empty until then — do not invent entries.
];

const achievementsConfig = {
  certificates,
};

export default achievementsConfig;
